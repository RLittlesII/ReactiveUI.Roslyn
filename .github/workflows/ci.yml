# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActionsSteps (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_ci --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: ci

on:
  push:
    branches:
      - main
      - next
    tags:
      - v*
  pull_request:
    branches:
      - main
      - next

jobs:
  Build:
    strategy:
      fail-fast: false
      matrix:
        os: [macOS-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          clean: 'false'
      - name: 🔨 Use .NET Core 6.0 SDK
        uses: actions/setup-dotnet@v1.8.2
        with:
          dotnet-version: '6.0.x'
          include-prerelease: true
      - name: 🎁 dotnet tool restore
        run: |
          dotnet tool restore
      - name: 🎁 Restore
        run: |
          dotnet nuke Restore --skip
      - name: ⚙ Build
        run: |
          dotnet nuke Build --skip
      - name: 🚦 Test
        run: |
          dotnet nuke Test --skip
      - name: Trigger Code Coverage Reports
        run: |
          dotnet nuke Trigger_Code_Coverage_Reports --skip
      - name: Generate Code Coverage Report Cobertura
        run: |
          dotnet nuke Generate_Code_Coverage_Report_Cobertura --skip
      - name: Generate Code Coverage Badges
        run: |
          dotnet nuke Generate_Code_Coverage_Badges --skip
      - name: Generate Code Coverage Summary
        run: |
          dotnet nuke Generate_Code_Coverage_Summary --skip
      - name: Generate Code Coverage Report
        run: |
          dotnet nuke Generate_Code_Coverage_Report --skip
      - name: 📦 Pack
        run: |
          dotnet nuke Pack --skip
